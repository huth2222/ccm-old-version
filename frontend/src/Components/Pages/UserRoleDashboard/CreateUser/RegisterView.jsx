import ProgressBar from "../../../CommonComponent/ProgressBar";

const RegisterView = () => {
  return (
    <div>
      <ProgressBar activeStepLabel={1} />
      <h1>Hello this is Register view</h1>
    </div>
  );
};

export default RegisterView;
